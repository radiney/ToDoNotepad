# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ToDoNotepad' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ToDoNotepad

  target 'ToDoNotepadTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ToDoNotepadUITests' do
    # Pods for testing
  end
  
  pod 'Firebase/Core'
  pod 'Firebase/Database'

end
